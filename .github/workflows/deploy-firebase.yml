name: Deploy B2C to Firebase Hosting

# Live Hosting은 main 푸시·스케줄·수동 실행만.
# 기능 브랜치에서 firebase deploy 하면 이후 main 스케줄이 덮어쓸 수 있음 → 배포 전에 main 머지.
#
# VITE_* 는 빌드 시 번들에 박힘. .env 는 gitignore 라 CI에 시크릿으로 넣어야 함.
# 없으면 지도 키가 빈 채로 배포되어 「어제 보이던 지도」가 사라진다.

on:
  push:
    branches:
      - main
  schedule:
    # 매시 정각 (UTC) — 시트 가격 변경 시 자동 sync + 배포
    - cron: '0 * * * *'
  workflow_dispatch:

concurrency:
  group: deploy-firebase-hosting
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - run: npm ci

      - name: Sync eSIM offers from Google Sheet
        if: github.event_name != 'push' && hashFiles('data/esim/sheets.json') != ''
        run: npm run sync:esim

      - name: Build
        env:
          VITE_NAVER_MAP_NCP_KEY_ID: ${{ secrets.VITE_NAVER_MAP_NCP_KEY_ID }}
        run: npm run build

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          projectId: airpick-reservation
          channelId: live
          target: airpick-b2c
